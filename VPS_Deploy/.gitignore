# Mosquitto — runtime data (created on VPS)
mosquitto/data/
mosquitto/log/

# Mosquitto — passwd (contains credentials, created on VPS)
mosquitto/config/passwd

# Node-RED — auto-save files
nodered/data/flows.json
nodered/data/flows_cred.json
nodered/data/package.json
nodered/data/node_modules/

# Secrets
.env

# Website (created on VPS)
website/
