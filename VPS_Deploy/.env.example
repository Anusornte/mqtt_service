# Copy ไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# cp .env.example .env

# Password สำหรับ user nodered (ใช้ใน healthcheck + Node-RED config)
NODERED_MQTT_PASS=Nongaom@1234

# Secret key สำหรับเข้ารหัส credentials ของ Node-RED
NR_CREDENTIAL_SECRET=change-this-to-a-random-64-char-string
